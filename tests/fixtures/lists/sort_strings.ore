fn main
  -- Sort strings lexicographically
  mut words := ["cherry", "apple", "banana", "date"]
  words = words.sort()
  print words.join(", ")

  -- Sort floats
  mut nums := [3.14, 1.0, 2.71, 0.5]
  nums = nums.sort()
  nums.each(n => print n)

  -- Dedup consecutive duplicates
  data := [1, 1, 2, 2, 2, 3, 1, 1]
  deduped := data.dedup()
  print deduped.join(" ")
