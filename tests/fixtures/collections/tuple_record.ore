type Point { x:Int, y:Int }

fn main
  -- Tuple auto-converts to Point in typed binding context
  p : Point := (3, 4)
  print p.x
  print p.y

  -- Regular record construction still works
  q := Point(x: 10, y: 20)
  print q.x
  print q.y
